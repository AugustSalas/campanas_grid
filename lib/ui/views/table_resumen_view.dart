import 'package:flutter/material.dart';


class TableResumenView extends StatelessWidget {
  const TableResumenView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink,
      child: const Center(
        child: Text('EL RESUMEN'),
      ),
    );
  }
}